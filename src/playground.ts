/**
 * Playground entry — dev-mode documentation app.
 *
 * Not part of the components/tokens public surface; carried in the
 * package so any HC1 product's picker can mount the same DS
 * playground without re-implementing it. Products should NOT ship
 * this to their production bundle.
 */

export { DesignSystemPlayground } from "./DesignSystemPlayground";
