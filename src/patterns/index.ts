// Composed patterns (worklist + filters, dashboard hero + KPIs, etc.) will
// live here as they are extracted. Empty in this pass — pattern documentation
// lives under `../docs/patterns/`.
export {};
