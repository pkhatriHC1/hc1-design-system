import { ComingSoonCard } from "../../layouts";

export function SettingsDoc() {
  return (
    <ComingSoonCard note="Settings template — two-pane layout, section list on the left, active section on the right. Forms follow the Forms pattern; destructive actions use CTA amber." />
  );
}
