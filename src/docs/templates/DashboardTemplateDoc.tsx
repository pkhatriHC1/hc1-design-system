import { ComingSoonCard } from "../../layouts";

export function DashboardTemplateDoc() {
  return (
    <ComingSoonCard note="Dashboard template — full-page composition of Navigation + IntelligenceHeader + Worklist + EvidenceBox. Ready-to-copy starting point for a new IQ module's landing view." />
  );
}
