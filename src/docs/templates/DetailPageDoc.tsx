import { ComingSoonCard } from "../../layouts";

export function DetailPageDoc() {
  return (
    <ComingSoonCard note="Detail page template — header with breadcrumb, tabbed body (Labs / Orders / Notes), care-plan zone, and an EvidenceBox terminal element per zone." />
  );
}
