import { ComingSoonCard } from "../../layouts";

export function IconsDoc() {
  return (
    <ComingSoonCard note="lucide-react icon set with hc1 sizing rules (12 / 14 / 16 / 20 / 24), stroke widths, and severity-icon mapping (AlertCircle → critical, Shield → medium, ShieldCheck → normal)." />
  );
}
