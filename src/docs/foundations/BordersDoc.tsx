import { ComingSoonCard } from "../../layouts";

export function BordersDoc() {
  return (
    <ComingSoonCard note="Border widths (hairline / focus / accent) and border colors (subtle / default / strong / focus). Includes the reserved btn-highlight strokes for a future pressed-state depth cue." />
  );
}
