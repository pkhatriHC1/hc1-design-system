import { ComingSoonCard } from "../../layouts";

export function AvatarDoc() {
  return (
    <ComingSoonCard note="Circular avatar with image or initials fallback. Sizes match control-height tokens." />
  );
}
