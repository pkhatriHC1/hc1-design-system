import { ComingSoonCard } from "../../layouts";

export function DashboardPatternDoc() {
  return (
    <ComingSoonCard note="Dashboard pattern — IntelligenceHeader with KPI values, worklist table, and side-rail evidence. The canonical layout for a landing view in any IQ module." />
  );
}
