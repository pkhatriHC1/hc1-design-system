import { ComingSoonCard } from "../../layouts";

export function FormsDoc() {
  return (
    <ComingSoonCard note="Form composition — label + input + hint + error. Grouped fieldsets, disclosure of dependent fields, and the ClinicalIQ constraint: no native <form>, use onClick / onChange." />
  );
}
