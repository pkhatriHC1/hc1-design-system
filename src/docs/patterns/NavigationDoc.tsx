import { ComingSoonCard } from "../../layouts";

export function NavigationDoc() {
  return (
    <ComingSoonCard note="Top-level and section-level navigation — sidebar for module scope, tabs for within-view scope, breadcrumbs for depth. Same primitive across every IQ module for muscle-memory consistency." />
  );
}
