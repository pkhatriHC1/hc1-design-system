import { ComingSoonCard } from "../../layouts";

export function FiltersDoc() {
  return (
    <ComingSoonCard note="Filter bar — Select + Input + StatusChip filter pills, with clear-all affordance. Reserved for above-table filtering; sidebar navigation lives in the Navigation pattern." />
  );
}
