import { ComingSoonCard } from "../../layouts";

export function TablesDoc() {
  return (
    <ComingSoonCard note="Table patterns — worklist rows, expandable detail rows (composed with Accordion), row actions, empty and loading states, and the tabular-nums-hc1 rule for every numeric cell." />
  );
}
